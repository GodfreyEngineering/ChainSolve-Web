# ── Frontend (Vite) ──────────────────────────────────────────────────────────
# Exposed to the browser; safe to commit as-is (protected by Supabase RLS)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ── Cloudflare Pages Functions (server-side only, never exposed to browser) ──
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

STRIPE_SECRET_KEY=sk_live_...          # or sk_test_... for dev
STRIPE_WEBHOOK_SECRET=whsec_...        # from Stripe → Webhooks → signing secret
STRIPE_PRICE_ID_PRO_MONTHLY=price_...  # Pro monthly recurring price
STRIPE_PRICE_ID_PRO_ANNUAL=price_...   # Pro annual recurring price
STRIPE_PRICE_ID_ENT_10_MONTHLY=price_...          # Enterprise 10 seats monthly
STRIPE_PRICE_ID_ENT_10_ANNUAL=price_...           # Enterprise 10 seats annual
STRIPE_PRICE_ID_ENT_UNLIMITED_MONTHLY=price_...   # Enterprise Unlimited monthly
STRIPE_PRICE_ID_ENT_UNLIMITED_ANNUAL=price_...    # Enterprise Unlimited annual

# ── Cloudflare Turnstile CAPTCHA (E2-2) ──────────────────────────────────────
# Omit or leave blank to disable CAPTCHA on auth forms (useful for local dev).
# VITE_TURNSTILE_SITE_KEY=0x4AAAAAA...

# ── AI Copilot (AI-1) ────────────────────────────────────────────────────────
# OpenAI API key for the AI Copilot server-side proxy.
# Set this in Cloudflare Pages secrets (production) or .dev.vars (local dev).
# OPEN_AI_API_KEY=sk-...
# AI_MODEL=gpt-4.1                     # Optional, default: gpt-4.1

# ── Observability (opt-in) ────────────────────────────────────────────────────
# Set VITE_OBS_ENABLED=true to enable browser-side error reporting.
# Disabled by default; enable in production after reviewing privacy implications.
# VITE_OBS_ENABLED=true
# VITE_OBS_SAMPLE_RATE=1.0  # 0.0–1.0 fraction of events to send
