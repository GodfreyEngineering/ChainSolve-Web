/**
 * termsVersion.ts — Current Terms & Conditions version (E2-3).
 *
 * Bump this when the ToS text changes. Users who accepted an older
 * version will see the acceptance gate again on next login.
 */

export const CURRENT_TERMS_VERSION = '1.0'
